<%= render GameMod::ShowComponent.new(
  currentAccountId: current_account.id,
  game: @game,
  gameCards: @game_cards,
  initialGameOver: @game_over,
  numOfCardsInDeck: @num_of_cards_in_deck,
  initialLeaderboard: @players,
  redirectToLobbyPath: @redirect_to_lobby_path
) %>